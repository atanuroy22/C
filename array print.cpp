#include<stdio.h>

int main()
{
int n,i,num[100],min=num[0],max=num[0];
printf("how many number you want to add\n");
scanf("%d",&n);
for(i=0;i<n;i++)
{
scanf("%d",&num[i]);
}
for(i=0;i<n;i++)
{
    printf("%d",num[i]);
}

for(i=0;i<n;i++)
{
	if(num[i]<max)
	{
	
	}
    
}
return (0);
}